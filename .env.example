DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler/DB?sslmode=require"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
AUTH_SECRET="generate-a-long-random-string"

# Stripe (https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Badlands Plan monthly subscription (cents). Optional Stripe Price ID overrides inline price_data.
BADLANDS_PLAN_PRICE_CENTS="999"
# STRIPE_PLAN_PRICE_ID="price_..."

# Admin panel password (used at /admin)
ADMIN_PASSWORD="changeme"

# Optional email notifications (https://resend.com)
RESEND_API_KEY=""
CONTACT_TO_EMAIL="info@badlandsbricks.com"

# Required on Vercel for MOC photo/PDF uploads (Storage → Blob)
BLOB_READ_WRITE_TOKEN=""
